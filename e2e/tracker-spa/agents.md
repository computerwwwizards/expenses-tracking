# E2E flow

User inserts its email into a email input then  presses the only submit that finds, then writes down a fake otp and submits immediately, then a list of 
recent workspaces are shown then we can choose one let's say the first and this sends us to a menu that has two anchors that can send us either to expenses or to incomes